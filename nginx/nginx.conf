upstream streamlit {
    server dashboard:8501;
}

server {
    listen 80;
    server_name bot.marklu.page;

    # Let's Encrypt ACME challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Streamlit needs WebSocket support
    location / {
        proxy_pass http://streamlit;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
    }

    location /_stcore/health {
        proxy_pass http://streamlit/_stcore/health;
    }
}

# Uncomment after running certbot to enable HTTPS:
# server {
#     listen 443 ssl;
#     server_name bot.marklu.page;
#
#     ssl_certificate     /etc/letsencrypt/live/bot.marklu.page/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/bot.marklu.page/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#
#     location / {
#         proxy_pass http://streamlit;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 86400;
#     }
#
#     location /_stcore/health {
#         proxy_pass http://streamlit/_stcore/health;
#     }
# }
